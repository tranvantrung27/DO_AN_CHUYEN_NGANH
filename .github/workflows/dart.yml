name: Flutter CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    # Đặt working-directory chung cho tất cả các bước bên dưới
    defaults:
      run:
        working-directory: ./herb_scan

    steps:
      - uses: actions/checkout@v4
      
      # Bước 1: Cài đặt Flutter SDK
      - uses: subosito/flutter-action@v2 
        with:
          # CỐ ĐỊNH PHIÊN BẢN GIỐNG TRÊN MÁY BẠN (Đã xóa dòng 3.x thừa)
          flutter-version: '3.29.2' 
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze project source
        # Đã chọn phiên bản có --no-fatal-infos để bỏ qua lỗi vặt
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test